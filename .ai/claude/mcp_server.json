{
  "mcpServers": {
    "claude-security": {
      "command": "python3",
      "args": [".ai/mcp/scripts/repo_agent_server.py"],
      "env": {
        "LOG_DIR": "logs/claude",
        "AUTO_MERGE": "identity-metadata-only",
        "YOLO_MODE": "identity-metadata-only",
        "MCP_AGENT_ROLE": "claude-security",
        "PROJECT_ROOT": "."
      },
      "description": "Read-only repository status compatibility adapter using the claude-security identity; legacy security and automation labels are metadata only and perform no scan or merge"
    }
  }
}
