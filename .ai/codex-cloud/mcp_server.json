{
  "mcpServers": {
    "codex-cloud-agent": {
      "command": "python3",
      "args": [".ai/mcp/scripts/repo_agent_server.py"],
      "env": {
        "LOG_DIR": "logs/codex-cloud",
        "AUTO_MERGE": "identity-metadata-only",
        "YOLO_MODE": "identity-metadata-only",
        "CODEX_CLOUD_API": "identity-metadata-only",
        "GITHUB_TOKEN": "identity-metadata-only",
        "MCP_AGENT_ROLE": "codex-cloud-agent",
        "PROJECT_ROOT": "."
      },
      "description": "Read-only repository status compatibility adapter using the codex-cloud-agent identity; legacy cloud, GitHub, and automation labels are metadata only and make no external calls"
    }
  }
}
