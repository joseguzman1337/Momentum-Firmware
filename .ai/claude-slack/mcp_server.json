{
  "mcpServers": {
    "claude-slack-agent": {
      "command": "python3",
      "args": [".ai/mcp/scripts/repo_agent_server.py"],
      "env": {
        "LOG_DIR": "logs/claude-slack",
        "AUTO_MERGE": "identity-metadata-only",
        "YOLO_MODE": "identity-metadata-only",
        "SLACK_BOT_TOKEN": "identity-metadata-only",
        "CLAUDE_API_KEY": "identity-metadata-only",
        "MCP_AGENT_ROLE": "claude-slack-agent",
        "PROJECT_ROOT": "."
      },
      "description": "Read-only repository status compatibility adapter using the claude-slack-agent identity; legacy automation, Slack, and Claude labels are metadata only and make no external calls"
    }
  }
}
