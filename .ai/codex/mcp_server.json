{
  "mcpServers": {
    "codex-agent": {
      "command": "python3",
      "args": [".ai/mcp/scripts/repo_agent_server.py"],
      "env": {
        "LOG_DIR": "logs/codex",
        "AUTO_MERGE": "identity-metadata-only",
        "YOLO_MODE": "identity-metadata-only",
        "OPENAI_API_KEY": "identity-metadata-only",
        "MCP_AGENT_ROLE": "codex-agent",
        "PROJECT_ROOT": "."
      },
      "description": "Read-only repository status compatibility adapter using the codex-agent identity; legacy Codex and automation labels are metadata only and make no external calls"
    }
  },
  "agentConfig": {
    "name": "Codex Agent",
    "description": "Read-only repository status compatibility adapter registered under the Codex identity",
    "status": "On-demand MCP; no background monitoring, code generation, PR submission, or merge execution",
    "capabilities": [
      "Read repository branch and HEAD",
      "Read scoped worktree status",
      "Run non-mutating git diff validation",
      "Describe the adapter's actual capability surface"
    ],
    "workflow": [
      "Start only when an MCP client launches it",
      "Respond only to explicit MCP tool calls",
      "Never mutate files or contact GitHub"
    ]
  }
}
