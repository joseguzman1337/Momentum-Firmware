{
  "mcpServers": {
    "deepseek-optimizer": {
      "command": "python3",
      "args": [".ai/mcp/scripts/repo_agent_server.py"],
      "env": {
        "LOG_DIR": "logs/deepseek",
        "INDIVIDUAL_PRS": "identity-metadata-only",
        "AUTO_MERGE": "identity-metadata-only",
        "YOLO_MODE": "identity-metadata-only",
        "MCP_AGENT_ROLE": "deepseek-optimizer",
        "PROJECT_ROOT": "."
      },
      "description": "Read-only repository status compatibility adapter using the deepseek-optimizer identity; legacy optimization, PR, and automation labels are metadata only"
    }
  }
}
