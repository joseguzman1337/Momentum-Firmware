#pragma once

// Wrapper that delegates to the upstream Desktop settings definitions to avoid
// duplicate enums and structs between Momentum and upstream.
#include <upstream/flipperzero-firmware/applications/services/desktop/desktop_settings.h>
