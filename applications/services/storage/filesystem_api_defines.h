#pragma once

// Wrapper delegating to upstream filesystem API defines to avoid duplicate
// enum/typedef definitions between Momentum and upstream.
#include <upstream/flipperzero-firmware/applications/services/storage/filesystem_api_defines.h>
