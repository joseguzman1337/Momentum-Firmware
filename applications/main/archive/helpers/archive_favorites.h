#pragma once

// Wrapper that delegates to the upstream Archive favorites helpers to avoid
// duplicate declarations between Momentum and upstream.
#include <upstream/flipperzero-firmware/applications/main/archive/helpers/archive_favorites.h>
