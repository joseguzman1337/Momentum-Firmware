#pragma once

// Wrapper that delegates to the upstream Archive browser view to avoid
// duplicate type and function definitions between Momentum and upstream.
#include <upstream/flipperzero-firmware/applications/main/archive/views/archive_browser_view.h>
