#pragma once

// Wrapper that delegates to the upstream Archive scene definitions to avoid
// duplicate enums and declarations between Momentum and upstream.
#include <upstream/flipperzero-firmware/applications/main/archive/scenes/archive_scene.h>
