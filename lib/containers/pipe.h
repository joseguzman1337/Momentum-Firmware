/**
 * @file pipe.h
 * Compatibility wrapper for the upstream toolbox pipe API.
 *
 * Momentum code should include this header, which delegates to
 * "toolbox/pipe.h" to avoid duplicate type and function definitions.
 */
#pragma once

#include <toolbox/pipe.h>
