#pragma once

// Wrapper delegating to upstream SubGHz device types to avoid duplicate
// SubGhzDeviceInterconnect definitions in SDK.
#include <upstream/flipperzero-firmware/lib/subghz/devices/types.h>
